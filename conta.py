class conta:
      def __init__(self,saldo)
          self.saldo = saldo
      def saca(self,valor)
          valor = x 
          saldo - x = saldofinal
      def depositar(self,deposita)
          deposita = y 
          y + saldofinal = saldo final
